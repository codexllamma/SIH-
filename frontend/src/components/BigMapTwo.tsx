import React, { useEffect, useRef, useState } from "react";
import { Stage, Layer, Line, Circle, Rect, Text } from "react-konva";

const stage=new Stage(); 

const layer=new Layer({stage.add(layer);}); 




